name: Publish Local IPTV Proxy APK

on:
  push:
    branches:
      - main
    paths:
      - 'android-sbsplus-proxy/**'
      - '.github/workflows/publish-localiptvproxy-release.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-sbsplus-proxy
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Build APK
        run: gradle assembleDebug --stacktrace

      - name: Prepare APK
        run: cp app/build/outputs/apk/debug/app-debug.apk ../LocalIPTVProxy-2.3.apk

      - name: Create or update GitHub Release
        working-directory: .
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG='localiptvproxy-v2.3'
          TITLE='Local IPTV Proxy 2.3'
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" LocalIPTVProxy-2.3.apk --clobber
          else
            gh release create "$TAG" LocalIPTVProxy-2.3.apk \
              --title "$TITLE" \
              --notes 'Android local IPTV aggregate playlist proxy. Fixes server startup UI/runtime diagnostics, keeps HTTP alive if mDNS fails, and adds /health.'
          fi
