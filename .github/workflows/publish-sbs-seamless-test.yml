name: Publish SBS Seamless Test APK

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/publish-sbs-seamless-test.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 9d764ab324cae339e094c524ae911a432e2d6131

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Build fallback test APK
        working-directory: android-sbsplus-proxy
        run: gradle assembleDebug --stacktrace

      - name: Prepare test APK asset
        run: cp android-sbsplus-proxy/app/build/outputs/apk/debug/app-debug.apk LocalIPTVProxy-3.4-fallback.apk

      - name: Upload separate test asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload localiptvproxy-v2.2 LocalIPTVProxy-3.4-fallback.apk --clobber
